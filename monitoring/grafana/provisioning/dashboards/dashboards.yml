apiVersion: 1

providers:
  - name: spring-boot-dashboard
    folder: TicketPing
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards

  - name: grafana-dashboard
    folder: TicketPing
    type: file
    options:
      path: /etc/grafana/provisioning/dashboards